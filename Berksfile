source 'http://api.berkshelf.com'

metadata

group :integration do
  cookbook 'httpd'
  cookbook 'selinux'
  cookbook 'iptables'
  cookbook 'webserver'
end

cookbook 'httpd', path: 'httpd'
cookbook 'selinux', path: 'selinux'
cookbook 'iptables', path: 'iptables'
cookbook 'webserver', path: 'webserver'
